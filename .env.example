## Sample environment configuration
## Copy this to `.env.development`, `.env.test` or `.env.production` as needed.
## Never commit your real Supabase keys; keep service keys secret.

NODE_ENV=development
PORT=3000

# Supabase credentials (mandatory for backend)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ANON_KEY=

# CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:4200,http://localhost:5173
ALLOW_ALL_ORIGINS=false
